namespace DotNetProratedSubscription
{
    public static class Subscription
    {
        public static decimal CalculateProratedSubscription(DateTime dateTime, decimal amount)
        {
            throw new NotImplementedException();
        }
    }
}
